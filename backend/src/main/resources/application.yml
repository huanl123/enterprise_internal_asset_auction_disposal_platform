spring:
  application:
    name: waidp-backend

  datasource:
    url: jdbc:mysql://localhost:3306/waidp_springboot_master?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=CONVERT_TO_NULL
    username: root
    password: '927933'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none  # 禁用自动建表
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  web:
    resources:
      static-locations: file:E:/Study/code/Java/enterprise_internal_asset_auction_disposal_platform/uploads/Voucher/

file:
  upload-dir: E:/Study/code/Java/enterprise_internal_asset_auction_disposal_platform/uploads/Voucher
  access-url: http://localhost

upload:
  path: E:/Study/code/Java/enterprise_internal_asset_auction_disposal_platform/uploads

server:
  port: 8084  # 修改为8084端口避免冲突
  servlet:
    context-path: /

jwt:
  secret: WAIDP_SECRET_KEY_2024_企业废旧资产内部拍卖与处置平台_SECRET_KEY_非常长
  expiration: 86400000  # 24小时（毫秒）

logging:
  level:
    com.waidp: debug
    com.waidp.service.impl.AssetServiceImpl: debug
    org.springframework.web: info
    org.springframework.web.servlet.DispatcherServlet: debug
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace